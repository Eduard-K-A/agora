# Optional local shell env. Keep secrets out of the renderer.
# The shell should read the Worker URL and any non-secret config from here if needed.

VITE_WORKER_BASE_URL=http://localhost:8787
VITE_AGORA_APP_ID=your_agora_app_id_here

